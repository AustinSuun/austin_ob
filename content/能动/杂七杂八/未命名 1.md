# 青岛大学 2019-2020 学年第 1 学期

## 人工智能课程试卷

**注：在考试结束后，试卷随答题纸一起上交。考试时间：120 分钟**

---

### 一、 简答题 【共 35 分】

1. **（7 分）** 简述启发式搜索中，全局择优搜索算法的基本思想。
    
2. 支持向量机 SVM 是机器学习中非常重要的算法，根据你的理解试图回答以下问题：
    
    - **1) （6 分）** 试解释 SVM 中的最大间隔准则。
        
    - **2) （6 分）** 简述 SVM 中核函数的功能。
        
3. **（8 分）** 简述 Adaboost 中级联分类器设计的基本思想。
    
4. **（8 分）** 试说明采用 ReLU 函数做激活函数训练深度神经网络的好处及缺点。
    

---

### 二、 线性回归和逻辑回归 【共 20 分】

1. 线性回归和逻辑回归分别解决了什么样的问题？
    
2. 假设 $\{x^{(i)}, y^{(i)}\}_{i=1, \dots, m}$ 为 $m$ 个训练数据。在线性回归中，我们实际上是解决了下面的优化问题。其中一种解决方案是梯度下降法。请简要描述该方法并给出下式中 $\theta$ 的更新规则。
    
    $$\min_{\theta} J(\theta) = \frac{1}{2} \sum_{i=1}^{m} (\theta^T x^{(i)} - y^{(i)})^2$$
    
3. 试说明（2）中得出的 $\theta$ 的更新方式存在什么问题？
    
4. 请列举影响梯度下降法收敛结果的几种因素。
    

---

### 三、 博弈树搜索 【共 15 分】

设有如下图所示的博弈树，其中最下面的数字是假设的每个节点的评估值，请对该博弈树作如下工作：

(注：参考图片 2 中的树状图)

1. 根据极大极小搜索计算各节点的倒推值。
    
2. 说明 $\alpha-\beta$ 剪枝的条件，并利用 $\alpha-\beta$ 剪枝技术剪去不必要的分枝，画出剪枝结果并说明采用的是 $\alpha$ 剪枝还是 $\beta$ 剪枝。
    
3. 试说明 $\alpha-\beta$ 剪枝的缺点。
    

---

### 四、 卷积神经网络 【共 15 分】

1. 简述卷积神经网络 CNN 的核心思想。
    
2. 假设构造一个 3 隐层的卷积神经网络，训练图像大小为 $64 \times 64$，第一个隐层（特征提取层 C1）采用 4 个卷积核，每个卷积核大小均为 $5 \times 5$，卷积步长为 1，考虑偏置项。试问：
    
    - **1)** 第一隐层应包含的神经元个数？
        
    - **2)** 第一隐层需确定的参数个数？
        
3. 如采用 sigmoid 映射函数进行池化，池化核大小为 $2 \times 2$，步长为 2，则经过第二隐层（特征映射层 S2）后，得到的映射图大小是多少？
    

---

### 五、 感知器分类准则 【共 15 分】

有两个样本：

$$W_1: x_{11} = (0, 0)^T, x_{12} = (0, 1)^T$$

$$W_2: x_{21} = (1, 0)^T, x_{22} = (1, 1)^T$$

试用感知器中单样本修正法对样本进行分类，写出最终收敛的判别函数。
